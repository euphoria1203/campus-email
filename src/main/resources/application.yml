spring:
  application:
    name: campus-mail
  datasource:
    url: jdbc:mysql://localhost:3306/campus_mail?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: 20051203wyy
    driver-class-name: com.mysql.cj.jdbc.Driver
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  servlet:
    multipart:
      enabled: true
      max-file-size: 50MB
      max-request-size: 100MB
  data:
    redis:
      port: 6379
      database: 0
      password: ""
      host: localhost

mybatis:
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.campusmail.entity
  configuration:
    map-underscore-to-camel-case: true

jwt:
  # JWT密钥必须至少256位(32字节)，这里使用Base64编码的密钥
  secret: Y2FtcHVzLW1haWwtand0LXNlY3JldC1rZXktMjAyNS1zdXBlci1zZWN1cmUtc3RyaW5n
  expiration-minutes: 120

# File Upload Configuration
file:
  upload-dir: ./uploads

# SMTP Server Configuration
smtp:
  port: 25
  thread-pool-size: 10

logging:
  level:
    com.campusmail: INFO
    com.campusmail.smtp: DEBUG
