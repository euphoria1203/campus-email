<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campusmail.mapper.UserMapper">
    <resultMap id="UserResult" type="com.campusmail.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="passwordHash" column="password"/>
        <result property="email" column="email"/>
        <result property="nickname" column="nickname"/>
        <result property="avatar" column="avatar"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.campusmail.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, email, nickname, status, created_at, updated_at)
        VALUES (#{username}, #{passwordHash}, #{email}, #{nickname}, #{status}, #{createdAt}, #{updatedAt})
    </insert>

    <select id="findByUsername" resultMap="UserResult">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <select id="findById" resultMap="UserResult">
        SELECT * FROM user WHERE id = #{id}
    </select>
</mapper>
